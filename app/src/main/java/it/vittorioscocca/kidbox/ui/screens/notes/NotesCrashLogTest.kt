package it.vittorioscocca.kidbox.ui.screens.notes

/**
 * Test pipeline log / crash report (solo debug).
 * Imposta [FORCE_CRASH_ON_OPEN] a `false` dopo aver verificato l'upload su Firestore.
 */
object NotesCrashLogTest {
    const val FORCE_CRASH_ON_OPEN = false
}
